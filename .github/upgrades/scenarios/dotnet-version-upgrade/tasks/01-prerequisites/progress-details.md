# Progress Details — 01-prerequisites

## Changes Made
- Updated `global.json`: SDK version changed from `8.0.x` to `10.0.x` (rollForward: latestFeature preserved)

## Validation
- `dotnet --version` reports `10.0.400` — correct SDK is active

## Files Modified
- `global.json`
